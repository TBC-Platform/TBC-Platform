# SPDX-License-Identifier: MIT
# Core server. Everything here is required.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
websockets>=12.0

# --- optional extras -------------------------------------------------------
# Object detection (the `look` intent). Install both, or set
# WALLE_VISION_ENABLED=0.
#   pip install tflite-runtime Pillow numpy
#
# faster-whisper instead of the whisper.cpp binary (better on NVIDIA GPUs):
#   pip install faster-whisper
#
# MQTT smart home backend (not needed for Home Assistant):
#   pip install aiomqtt
